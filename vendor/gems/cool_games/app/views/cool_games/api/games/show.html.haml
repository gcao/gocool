%h3

#gameContainer

:coffeescript
  $(document).ready ->
    id = "#{params[:id]}"
    url = urls.api.games + "/" + id + ".json"
    url = addAuthToUrl(url)

    $.ajax url,
      dataType: 'jsonp'
      crossDomain: true
      success: (response) ->
        handleResponse response,
          before: -> console.log url
          callback: -> showGame(response.body, response.user)

- content_for :footer do
  = render "cool_games/shared/jsgameviewer"
  = javascript_include_tag "cool_games/api/player"

