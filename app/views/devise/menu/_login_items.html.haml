- if user_signed_in?
  %li
    = current_user.username
  %li
    = link_to(t('discuz.logout'), destroy_session_path, :method => :delete)
- else
  %li
    = link_to(t('discuz.login'), new_session_path)
