:javascript
  var environment = "#{RAILS_ENV}";
  var controller_name = "#{params[:controller]}";
  var gameUrl = "#{new_game_url}";
  var playerSuggestUrl = "#{suggest_players_path}";

  function logMessage(message) {
    if (environment == 'development') console.log(message);
  }
